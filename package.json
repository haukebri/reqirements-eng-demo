{
  "name": "requirements-ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "start:prod": "cd backend && npm run build && node dist/src/db/migrate.js && node dist/src/index.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}
